/**
 * Run this seed with:
 * pnpm -F db seed ./src/seed/core.ts
 */

const main = async () => {
  console.log("Seeding core data; nothing to do");
  return Promise.resolve();
};

void main();
